# True-random-number-generator-using-webcam-frames-and-SHA-256-hashing
**Webcam Random Number Generator** is a Python tool that generates true random numbers using live webcam frames hashed with SHA-256. Since every frame differs slightly due to light and noise, the output is genuinely unpredictable rather than pseudo-random.
